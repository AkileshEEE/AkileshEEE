#include<stdio.h>
void main()
{
    int arr1[]={5,7,9,3,6,2};
    int arr2[]={1,2,5,-1,0,9};
    int n1=size of (arr 1)/size of (arr1[0]);
    int n2=size of (arr 2)/size of (arr2[0]);
    for (int i-1;i<n1;i++)
    {
        if (arr[i]>max1)
        {
            max1=arr 1[i];
    }
}
for(inti=1;i<n2;i++)
{
    if (arr2[i]<min2)
    {
        min2=arr2[i];
    }
}
int product max1*min2;
printf("max element in first array is%d\n",max1);
printf("min element in second array is %d\n",min2);
printf("product of the two is %d\n",product);
}
