#include<stdio.h>
int main()
{
    int a,b,c,d,temp;
    
    //input the four sides of the rectangle
    printf("Enter rectangle four sides of length:");
    scanf("%d %d %d %d",&a,&b,&c,&d);
    
    //sort the four sides in non decreasing order
    if(a>b)[temp=a;a=b;b=temp;]
    if(c>d)[temp=c;c=d;d=temp;]
    if(a>c)[temp=a;a=c;c=temp;]
    if(b>d)[temp=b;b=d;d=temp;]
    if(b>c)[temp=b;b=c;c=temp;]
    
    //check if the sides can form a rectangle
    if(a==b&&c==d)
    {
        printf("yes\n");
    }
    else
    {
        printf("No\n");
    }
    //input the four sides of the rectangle 
    printf("Enter rectangle four sides of length:");
    scanf("%d %d %d %d",&a,&b,&c,&d);
    //sort the four sides in non-decreasing order
    if(a>b)[temp=a;a=b;b=temp;]
    if(c>d)[temp=c;c=d;d=temp;]
    if(a>c)[temp=a;a=c;c=temp;]
    if(b>d)[temp=b;b=d;d=temp;]
    if(b>c)[temp=b;b=c;c=temp;]
    
    //check if the sides can form a rectangle
    
    if(a==b&&c==d);
    {
        printf("yes\n");
    }
    else
    {
        printf("No\n");
    }
}


